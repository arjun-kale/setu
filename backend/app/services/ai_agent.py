"""AI agent orchestration (LangGraph + RAG with Pinecone)."""

# LangGraph: orchestrate tools (scheme search, eligibility), multi-step flows
# Pinecone: vector search for RAG over scheme docs/FAQs

# def run_agent(session_id: str, message: str, context: dict) -> str: ...
